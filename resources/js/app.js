import './bootstrap';
import 'bootstrap';
import '../scss/app.scss';
import '@fortawesome/fontawesome-free/css/all.css';

// Livewire will be auto-initialized

// Chart.js will be loaded per-component via @push('scripts')
