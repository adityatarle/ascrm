# 📤 FTP Upload Guide - Complete File List

## ⚠️ CRITICAL: Run These Commands on Server AFTER Upload

```bash
# Install PHP dependencies
composer install
composer require barryvdh/laravel-dompdf
composer require maatwebsite/excel
php artisan vendor:publish --provider="Barryvdh\DomPDF\ServiceProvider"

# Install NPM dependencies
npm install
npm install chart.js --save

# Build assets
npm run build

# Clear caches
php artisan view:clear
php artisan cache:clear
php artisan config:clear
php artisan route:clear

# Optimize for production
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

---

## 📁 FILES TO UPLOAD

### 1. NEW FILES - Dashboard
```
app/Livewire/Dashboard/Dashboard.php
resources/views/livewire/dashboard/dashboard.blade.php
```

### 2. NEW FILES - Reports
```
app/Livewire/Reports/ReportsPage.php
resources/views/livewire/reports/reports-page.blade.php
app/Http/Controllers/Reports/ReportExportController.php
resources/views/reports/export-pdf.blade.php
resources/views/reports/partials/sales-pdf.blade.php
resources/views/reports/partials/orders-pdf.blade.php
resources/views/reports/partials/payments-pdf.blade.php
resources/views/reports/partials/products-pdf.blade.php
resources/views/reports/partials/dealers-pdf.blade.php
resources/views/reports/partials/dispatches-pdf.blade.php
resources/views/reports/partials/gst-pdf.blade.php
```

### 3. NEW FILES - Components
```
resources/views/components/report-header.blade.php
```

### 4. MODIFIED FILES
```
routes/web.php
resources/scss/app.scss
resources/js/app.js
resources/views/layouts/app.blade.php
resources/views/components/layouts/app.blade.php
composer.json
package.json
```

---

## 📋 UPLOAD CHECKLIST

### Step 1: Upload New Directories
- [ ] `app/Livewire/Dashboard/` (entire folder)
- [ ] `app/Livewire/Reports/` (entire folder)
- [ ] `app/Http/Controllers/Reports/` (entire folder)
- [ ] `resources/views/livewire/dashboard/` (entire folder)
- [ ] `resources/views/livewire/reports/` (entire folder)
- [ ] `resources/views/reports/` (entire folder with partials subfolder)
- [ ] `resources/views/components/` (upload report-header.blade.php)

### Step 2: Upload Modified Files
- [ ] `routes/web.php`
- [ ] `resources/scss/app.scss`
- [ ] `resources/js/app.js`
- [ ] `resources/views/layouts/app.blade.php`
- [ ] `resources/views/components/layouts/app.blade.php`
- [ ] `composer.json`
- [ ] `package.json`

### Step 3: Run Commands on Server (see above)

---

## 🚫 DO NOT UPLOAD

- `vendor/` folder (run `composer install` on server)
- `node_modules/` folder (run `npm install` on server)
- `.env` file (keep your existing one)
- `public/build/` (will be generated by `npm run build`)

---

## ✅ QUICK UPLOAD SUMMARY

**New Files:** 13 files
**Modified Files:** 7 files
**New Folders:** 3 folders

**Total to Upload:** 20 files/folders

---

## 🔍 VERIFICATION AFTER UPLOAD

1. Dashboard loads with charts ✓
2. Reports page shows all 7 tabs ✓
3. Print button works ✓
4. PDF export downloads ✓
5. Excel export downloads ✓
6. Charts display correctly ✓
7. Masters menu visible in sidebar ✓
8. Filters work on reports ✓

---

## 📝 NOTES

- All file paths are relative to project root
- Maintain folder structure when uploading
- After upload, run all commands in "CRITICAL" section above
- If charts don't work, ensure `npm run build` completed successfully
- If exports fail, ensure DomPDF and Excel packages are installed

